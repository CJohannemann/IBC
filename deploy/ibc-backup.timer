# Reference copy - installed by install-service.sh.

[Unit]
Description=Nightly IBC database backup

[Timer]
OnCalendar=*-*-* 03:15:00
# If the server was off at 03:15, run once it comes back rather than skipping.
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
